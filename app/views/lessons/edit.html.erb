<h2 class="mb-4"><i class="bi bi-pencil-fill"></i> レッスン編集 — <%= @course.title %></h2>
<div class="card" style="max-width:600px">
  <div class="card-body"><%= render "form", lesson: @lesson %></div>
</div>
